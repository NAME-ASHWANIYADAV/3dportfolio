import React from "react";
import Tilt from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className="xs:w-[250px] w-full">
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className="w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card"
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className="bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col"
      >
        <img
          src={icon}
          alt="web-development"
          className="w-16 h-16 object-contain"
        />

        <h3 className="text-white text-[20px] font-bold text-center">
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>ORIGIN</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className="mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]"
      >
        I am a dedicated MERN stack developer with a passion for creating
        dynamic, scalable, and efficient web applications. With expertise in
        MongoDB, Express.js, React, and Node.js, I specialize in building
        end-to-end solutions that are optimized for both functionality and user
        experience. My technical background includes front-end development,
        where I ensure a responsive and interactive user interface, and back-end
        development, where I focus on creating secure and high-performance APIs.
        Currently, I am expanding my skill set to include blockchain technology,
        recognizing its potential to bring enhanced security, transparency, and
        decentralization to applications. This journey involves studying and
        working with smart contracts, decentralized applications (DApps), and
        the principles of blockchain architecture. My goal is to merge my
        knowledge of the MERN stack with blockchain to deliver innovative
        solutions that address modern challenges in web development and beyond.
      </motion.p>

      <div className="mt-20 flex flex-wrap gap-10">
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
